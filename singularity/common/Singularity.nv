Bootstrap: docker
FROM:  docker://nvidia/cuda:!{CUDA_VERSION}-cudnn!{CUDNN_MAJOR_VERSION}-devel-ubuntu!{UBUNTU_VERSION}
%post
    apt-get update
    apt-get install -y --no-install-recommends build-essential
    apt-get clean
    rm -rf /var/lib/apt/lists/*
%runscript
	/bin/bash

